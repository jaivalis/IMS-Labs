function quantizeFeatures( voc )
% QUANTISIZEFEATURES 
%   represent each image as a collection of visual words
%   extracts feature descriptors (SIFT) and then assign each descriptor to
%   the closest visual word from the vocabulary.

end